# Execution Plans

This directory holds dated execution plans created during implementation work.

- Treat these files as historical implementation notes unless a current task explicitly points to one.
- Runtime and operator guidance lives in the top-level docs (`README.md`, `docs/guides/`, and `docs/operations/`), not in these plan files.
- New multi-step work should still create or update a short plan here before code changes begin.
